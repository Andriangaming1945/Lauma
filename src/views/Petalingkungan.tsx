import React from 'react';
import Interaktif from '../components/Interaktif';

const Guide: React.FC = () => {
  return (
    <div>
       <Interaktif />
       {/* The Interaktif component is used here, but you can replace it with any other component
           or content that you want to display in this view. */}
       {/* If you want to add more components or content, you can do so here. */}
       {/* For example, you could add a header, footer, or any other section as needed. */}
    </div>
  );
};

export default Guide;
